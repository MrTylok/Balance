/**
 * Controller to supply random amount of images to frontend
 * @param {*} req
 * @param {*} res
 * @returns
 */
const controller = (req, res) => {
  return res.sendStatus(200);
};

module.exports = controller;
